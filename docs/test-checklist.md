# CP2 — Checklist kiểm thử LearningRadar

- [x] Mở được prototype bằng `npm run dev` trong thư mục `prototype/`.
- [x] Bôi đen một đoạn slide **hoặc** dán đoạn đó vào ô “Đoạn tài liệu cần hỏi”.
- [x] Nhập câu hỏi và bấm **Gửi câu hỏi cho AI Tutor**.
- [x] Kết quả mock hiển thị đủ: giải thích, citation và nhãn kiến thức.
- [x] Bấm **Chưa hiểu** tạo ticket mới; mở Dashboard thấy ticket, lý do và đoạn text đã chọn.
- [x] Làm quiz 5 câu, đạt từ 3/5: hiện kết quả pass và không tạo ticket.
- [x] Làm quiz dưới 3/5: hiện kết quả fail; mở Dashboard thấy ticket có điểm quiz.
- [x] Trên Dashboard, lọc được trạng thái ticket và TA đổi được trạng thái ticket.

Đã kiểm: prototype khởi chạy và render bằng Chromium headless; `npm run build` chạy không lỗi. Cả nhóm nên chạy lại 2 nhánh demo trước khi gặp TA để xác nhận trên máy trình chiếu.
